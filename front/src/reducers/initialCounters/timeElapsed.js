import { initialTimeElapsed } from '../constants';

const timeElapsed = (state = initialTimeElapsed, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default timeElapsed;
